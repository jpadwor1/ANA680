{
  "name": "flask-vite-heroku",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd repo/client && npm ci && npm run build && rm -rf ../server/static && mkdir -p ../server/static && cp -r dist/* ../server/static/"
  },
  "cacheDirectories": [
    "client/node_modules"
  ]
}