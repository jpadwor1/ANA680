{
  "name": "flask-vite-heroku",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm --prefix repo/client ci --include=dev && npm --prefix repo/client run build && rm -rf repo/server/static && mkdir -p repo/server/static && cp -r repo/client/dist/. repo/server/static/"
  }
}
